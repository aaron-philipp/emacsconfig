# Emacs Configuration

Personal Emacs configuration using modern `use-package` declarations.

## Features

- **Claude Code** - AI pair programming with Claude via agent-shell + ACP
- **Dashboard** - Custom startup screen with quick links and recent files
- **Zenburn Theme** - Easy on the eyes color scheme
- **Nerd Icons** - Beautiful icons throughout the UI
- **Projectile** - Project management and navigation
- **Ivy/Counsel/Swiper** - Fast completion and selection narrowing
- **Company** - Code autocompletion
- **LSP Mode** - Language Server Protocol support
- **Python** - Elpy and Anaconda mode for Python development
- **Web** - Web mode with npm integration
- **Org Mode** - Notes and agenda with timeline
- **Fast Startup** - Deferred loading for sub-1-second startup

## Installation

### Prerequisites

For Claude Code integration, install the CLI and ACP wrapper:

```bash
# Install Claude Code CLI
# Option 1: Standalone installer (recommended)
# Download from https://claude.ai/download

# Option 2: npm
npm install -g @anthropic-ai/claude-code

# Option 3: Homebrew (macOS)
brew install anthropic/tap/claude-code

# Then install the ACP wrapper (requires Node.js)
npm install -g @zed-industries/claude-code-acp
```

### Quick Install

```bash
# Backup existing config
mv ~/.emacs.d ~/.emacs.d.bak
mv ~/.emacs ~/.emacs.bak

# Clone this repo
git clone https://github.com/aaron-philipp/emacsconfig.git

# Copy to home directory
cp -r emacsconfig/.emacs.d ~/
```

### Manual Install

1. Copy `.emacs.d/` directory to your home folder
2. Start Emacs - packages will auto-install on first launch
3. Run `M-x nerd-icons-install-fonts` to install icon fonts

## Structure

```
.emacs.d/
├── init.el          # Main configuration
├── early-init.el    # Early init for performance (Emacs 27+)
├── banner.txt       # ASCII art for dashboard
└── custom.el        # Generated by Customize (gitignored)
```

## Key Bindings

### Claude Code (`C-c c` prefix)

| Key | Function |
|-----|----------|
| `C-c c c` | Start agent-shell with Claude |
| `C-c c n` | New agent-shell session |

### General

| Key | Function |
|-----|----------|
| `C-c p` | Projectile prefix |
| `C-s` | Swiper (search in buffer) |
| `C-r` | Swiper backward |
| `F8` | Toggle Neotree |
| `M-x` | Counsel M-x |
| `C-x C-f` | Counsel find files |
| `C-x b` | Ivy switch buffer |
| `C-c g` | Counsel git (find files in repo) |
| `C-c j` | Counsel git grep (search in repo) |
| `C-c l` | LSP prefix |

## Dashboard Quick Links

- **Repos** - Opens GitHub
- **Jira** - Opens Atlassian
- **Gmail** - Opens email

## Customization

Edit `.emacs.d/init.el` to customize. The config is organized into sections:

1. Package Management
2. General Settings
3. Theme
4. Icons
5. Dashboard
6. Project Management
7. Navigation & Search (Ivy/Counsel/Swiper)
8. Autocompletion (Company)
9. Programming (General, Python, Web)
10. LSP
11. Org Mode
12. Modeline
13. AI - Claude Code
14. Fun Stuff

## Requirements

- Emacs 28+ (29+ recommended for `:vc` use-package support)
- Git (for package installation)
- Claude Code CLI (for AI features)

## License

MIT
